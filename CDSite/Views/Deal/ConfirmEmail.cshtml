@model CDSite.Models.DealRegisterViewModel
@{
    ViewBag.Title = "Confirm Email";
}

<h2>@ViewBag.Title</h2>
<div>
    <p>
        @if (String.IsNullOrEmpty(Model.OfferCode))
        {
           <p class="warning"> Sorry. No codes available for this product :( </p>
        }
        else {
           <p> Here is your code: @Model.OfferCode</p>
                }
    </p>
</div>
